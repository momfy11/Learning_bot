# Configuration

Place configuration template files here.

⚠️ **Important**: Never commit files with actual secrets or API keys. Use `.env` files and add them to `.gitignore`.

## Example

Create a `config.example.json` or `.env.example` file with placeholder values that users can copy and fill in with their actual credentials.
