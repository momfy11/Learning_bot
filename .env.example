# ===========================================
# Environment Configuration for Learning Bot
# ===========================================
# Copy this file to .env and fill in your values
# ⚠️ NEVER commit .env to version control - it contains secrets!

# -----------------
# Database Settings
# -----------------
# PostgreSQL format: postgresql+asyncpg://user:password@host:port/database
# Local Development: postgresql+asyncpg://postgres:yourpassword@localhost:5432/learning_bot
# Docker Compose: postgresql+asyncpg://postgres:yourpassword@db:5432/learning_bot
DATABASE_URL=postgresql+asyncpg://your_db_user:your_db_password@localhost:5432/learning_bot

# -----------------
# JWT Authentication
# -----------------
# Generate a secret key using: openssl rand -hex 32
SECRET_KEY=your-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# -----------------
# LLM API Settings
# -----------------
# We use Mistral API (free tier available)
# Get your API key from: https://console.mistral.ai/
MISTRAL_API_KEY=your-mistral-api-key-here
MISTRAL_MODEL=mistral-small-2506

# -----------------
# RAG Settings
# -----------------
# Embedding model for document vectorization
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Maximum tokens for LLM context window
MAX_CONTEXT_TOKENS=2000

# Number of document chunks to retrieve per query
TOP_K_RESULTS=3

# -----------------
# Voice Settings
# -----------------
# Speech-to-text uses browser's Web Speech API (no API key needed)
# Response playback uses browser's Speech Synthesis API

# -----------------
# CORS Settings
# -----------------
# Frontend URL for CORS (update for production domain)
FRONTEND_URL=http://localhost:5173

# -----------------
# File Upload Settings
# -----------------
# Maximum file size for document uploads
MAX_UPLOAD_SIZE_MB=50
# Supported file formats
ALLOWED_EXTENSIONS=pdf,txt,epub
# Directory where uploads are stored
UPLOAD_DIR=uploads
